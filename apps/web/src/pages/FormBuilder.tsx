import { FormBuilderShell } from "@/components/form-builder/FormBuilderShell";

/** Route entry for /admin/forms/:id/builder — the Google-Forms-style form builder. */
export function FormBuilder() {
  return <FormBuilderShell />;
}
